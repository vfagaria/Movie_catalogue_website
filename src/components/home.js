import React from "react";

const MovieHome = (movie) => {
  return (<>
<img className="image" src={movie.Poster} alt="movie" ></img>

  <h1>Hello this is move</h1>
  
  </>);
};

export default MovieHome;
